<div class="container">
    <div class="row row-cols-2">
        {{#each tracks}}
        <div class="col-6">
            {{this.name}}
        </div>
        <div class="col-6">
            <audio src={{this.preview_url}} controls></audio>
        </div>
        {{/each}}
    </div>
</div>